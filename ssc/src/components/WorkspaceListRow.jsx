import React from 'react';
import PropTypes from 'prop-types';

const WorkspaceListRow = ( { workspace } ) => {
    return (
        <>

        </>
    );
};

WorkspaceList.propTypes = {
    workspace: PropTypes.string
};

export default WorkspaceListRow;